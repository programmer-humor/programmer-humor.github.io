{
  "name": "programmer-humor-feed",
  "private": true,
  "scripts": {
    "build": "node scripts/prerender.mjs",
    "build:local": "node scripts/prerender.mjs --local",
    "preview": "python3 -m http.server 4173 -d dist"
  },
  "engines": {
    "node": ">=18"
  }
}
